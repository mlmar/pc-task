export type Performance = {
    name: string,
    imgUrl: string,
    description: string,
    spotifyUrl?: string,
    instagramUrl?: string,
    youtubeUrl?: string,
    websiteUrl?: string,
    soundcloudUrl?: string
}